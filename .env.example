# Required — used to protect organizer endpoints
ADMIN_SECRET=change-this-to-a-random-secret

# Optional — Vercel KV is auto-injected by Vercel when linked.
# Without these, the app uses an in-memory store for local development.
KV_URL=
KV_REST_API_TOKEN=
